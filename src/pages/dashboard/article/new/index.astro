---
import CreateArticleForm from "@/components/Dashboard/Article/DashboardCreateArticleForm"
import DashboardLayout from "@/layouts/DashboardLayout.astro"

const user = Astro.locals.user

if (!user?.role?.includes("admin" || "author")) {
  return Astro.redirect("/")
}
---

<DashboardLayout
  locale="id"
  seo={{
    title: "Create Article",
    description: "Create Article",
    canonical: `${import.meta.env.PUBLIC_SITE_URL}/dashboard/article/new`,
    openGraph: {
      basic: {
        title: `Create Article | `,
        url: `${import.meta.env.PUBLIC_SITE_URL}/dashboard/article/new`,
        type: "website",
        image: "",
      },
      optional: {
        description: "Create Article",
        locale: "ID",
      },
    },
  }}
>
  <div class="mx-4 flex w-full flex-col">
    <div class="mb-[100px] mt-4 flex items-end justify-end">
      <div class="flex-1 space-y-4">
        <CreateArticleForm session={user} client:load />
      </div>
    </div>
  </div></DashboardLayout
>
