---
import CreateVideoEmbedForm from "@/components/Dashboard/VideoEmbed/DashboardCreateVideoEmbedForm"
import DashboardLayout from "@/layouts/DashboardLayout.astro"

const user = Astro.locals.user

if (!user?.role?.includes("admin" || "author")) {
  return Astro.redirect("/")
}
---

<DashboardLayout
  locale="id"
  seo={{
    title: "Create Video Embed",
    description: "Create Video Embed",
    canonical: `${import.meta.env.PUBLIC_SITE_URL}/dashboard/video-embed/new`,
    openGraph: {
      basic: {
        title: `Create Video Embed | `,
        url: `${import.meta.env.PUBLIC_SITE_URL}/dashboard/video-embed/new`,
        type: "website",
        image: "",
      },
      optional: {
        description: "Create Video Embed",
        locale: "ID",
      },
    },
  }}
>
  <div class="mx-4 flex w-full flex-col">
    <div class="mb-[100px] mt-4 flex items-end justify-end">
      <div class="flex-1 space-y-4">
        <CreateVideoEmbedForm currentUser={user} client:load />
      </div>
    </div>
  </div>
</DashboardLayout>
